{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install",
            "type": "shell",
            "command": "pip install .",
            "group": "build"
        },
        {
            "label": "PyTest",
            "type": "shell",
            "command": "pytest --cov=py_tresos --cov-report term-missing",
            "group": "build"
        }
    ]
}